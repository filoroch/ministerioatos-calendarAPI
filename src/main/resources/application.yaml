spring:
  application:
    name: calendarAPI
  profiles:
    active: dev
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui
    enabled: true
  scalar:
    enabled: true
    path: /scalar
